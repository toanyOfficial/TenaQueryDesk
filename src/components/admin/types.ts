export type AdminConnection = Readonly<{ id: number; connectionKey: string; displayName: string; dbType: "mysql"; active: boolean; connectionStatus: "healthy" | "failed" | "unknown"; checkedAt: string | null }>;
export type AdminSchemaState = Readonly<{ connectionId: number; versionNo: number | null; status: "success" | "processing" | "failed" | "missing"; generatedAt: string | null; tableCount: number | null; viewCount: number | null; schemaHash: string | null; filesPresent: boolean }>;
export type AdminFailure = Readonly<{ id: number; connectionId: number; kind: "gpt" | "sql"; preview: string; requestType?: string; errorMessage: string; executionMs?: number | null; analysisHistoryId?: number | null; createdAt: string }>;
export type PermissionSummary = Readonly<{ connectionId: number; checkedAt: string; isReadOnly: boolean; riskLevel: "safe" | "warning" | "critical" | "unknown"; hostRestriction: "restricted" | "wildcard" | "review" | "unknown"; accountLabel: string; allowedPrivileges: ReadonlyArray<string>; riskyPrivileges: ReadonlyArray<string>; unknownPrivileges: ReadonlyArray<string>; hasGlobalPrivileges: boolean; hasCrossDatabasePrivileges: boolean; metadataReadable: boolean | null; reasons: ReadonlyArray<string>; recommendations: ReadonlyArray<string> }>;
export type OpenAiStatus = Readonly<{ configured: boolean; model: string | null; timeoutMs: number | null; maxOutputTokens: number | null; maxRetries: number | null; issues: ReadonlyArray<string>; lastRequest: Readonly<{ status: "success" | "failed"; createdAt: string; errorMessage?: string }> | null }>;
